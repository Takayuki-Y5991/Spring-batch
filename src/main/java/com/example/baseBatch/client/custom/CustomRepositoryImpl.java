package com.example.baseBatch.client.custom;

import org.springframework.stereotype.Repository;

@Repository
public class CustomRepositoryImpl implements CustomRepository {

//    public Object getArrearsBooks() {
//        var options = select(bookEntity.title, memberEntity.name, memberEntity.email)
//                .from(bookEntity)
//                .join(bookRentalEntity).on(bookEntity.bookId, equalTo(bookRentalEntity.bookId, ))
//                .where(bookEntity.status, isEqualTo(2))
//                .build()
//                .render(MYBATIS3);
//    }
}
